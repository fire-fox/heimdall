version: '3'

services:
  heimdall:
    image: linuxserver/heimdall:latest
    container_name: heimdall
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Lima
    volumes:
      - heimdall_data:/config
    restart: always

volumes:
  heimdall_data:
